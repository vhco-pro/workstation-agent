version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gocritic

formatters:
  enable:
    - gofumpt
    - goimports
    - gci
    - gofmt

# Reference: https://golangci-lint.run/usage/configuration/
